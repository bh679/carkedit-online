// CarkedIt Online — community links
//
// Where players go to talk to us. Kept here rather than inline in a component so
// the invite is changed in exactly one place when the server link is rotated.
'use strict';

// The CarkedIt community server. Set this to '' to pull every Discord link from
// the app at once — an empty invite renders nothing rather than a dead link.
export const DISCORD_INVITE_URL = 'https://discord.gg/s286u4dGTn';

/** Prompt shown to a first-time host in the online lobby. */
export const DISCORD_PROMPT = 'Have any questions? Jump in the discord and chat with the developers!';
